# [アルゴリズム・データ構造名]

<!-- 1〜3行でアルゴリズム／データ構造の概要を書く。何を解くものか、どういう場面で使うかを明示する。 -->

## アルゴリズム

<!-- アルゴリズムの動作原理を説明する。
     - データ構造の場合：内部表現と各操作の実現方法
     - アルゴリズムの場合：手順・漸化式・証明の概要
     数式は LaTeX で書く。 -->

$$
\text{ここに漸化式や定義式}
$$

## 計算量

<!-- 構築・各操作ごとに時間/空間計算量をまとめる。
     アルゴリズムの場合は最良・平均・最悪を書く。 -->

| 操作 | 時間計算量 | 空間計算量 |
|---|---|---|
| 構築 | $O(?)$ | $O(?)$ |
| クエリ | $O(?)$ | $O(1)$ |

<!-- アルゴリズムの場合はこちらを使う -->
<!--
| | 時間 | 空間 |
|---|---|---|
| 最良 | $O(?)$ | $O(?)$ |
| 平均 | $O(?)$ | $O(?)$ |
| 最悪 | $O(?)$ | $O(?)$ |
-->

## インターフェース

```cpp
// インクルードパス
#include "toolbox/.../xxx.hpp"

// 関数・クラスのシグネチャを書く
```

### 主要な操作

<!-- 各メソッド・関数について、引数・戻り値・制約・計算量を書く。 -->

- `操作名(引数)` — 説明。計算量 $O(?)$。
  - 制約：...

## 使用例

```cpp
#include "toolbox/.../xxx.hpp"

// 具体的な使い方を示す最小限のコード
```

## 実装上の注意

<!-- 実装特有の挙動、他実装との差異、既知の制限、今後の課題などを書く。
     特になければ削除してよい。 -->

## 参考文献

<!-- 参考にした論文・書籍・URL を列挙する。
     特になければ削除してよい。 -->
